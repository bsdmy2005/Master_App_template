# Clerk Authentication
# Get these from https://clerk.com/dashboard
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# Optional: Clerk webhook secret for syncing user data
# CLERK_WEBHOOK_SECRET=whsec_...

# Database (Render PostgreSQL)
# Get this from Render Dashboard > Your PostgreSQL > Connection > External Database URL
DATABASE_URL=postgresql://[USER]:[PASSWORD]@[HOST].frankfurt-postgres.render.com/[DATABASE]?ssl=true

# Object Storage (Cloudflare R2)
# Get these from Cloudflare Dashboard > R2 > Manage R2 API Tokens
R2_ACCESS_KEY_ID=your-access-key-id
R2_SECRET_ACCESS_KEY=your-secret-access-key
R2_ENDPOINT=https://[ACCOUNT-ID].r2.cloudflarestorage.com
R2_BUCKET_NAME=your-bucket-name
# Public URL for serving files (optional - leave commented for private access-controlled storage)
# Files will be accessed via presigned URLs which expire after a set time
# R2_PUBLIC_URL=https://pub-your-bucket-id.r2.dev

# Email (Postmark) - Optional
# Get these from https://postmarkapp.com
# POSTMARK_API_TOKEN=your-api-token
# POSTMARK_FROM_EMAIL=noreply@yourdomain.com
